package model;

public class EnemySkeleton extends Enemy{
	
	public EnemySkeleton(int ID, int score, int value, double speed, double attackRate, int damage, int health, int spawnScore) {
		super(ID, score, value, speed, attackRate, damage, health, spawnScore);
	}

}
